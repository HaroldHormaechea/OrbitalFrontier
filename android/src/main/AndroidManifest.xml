<?xml version="1.0" encoding="utf-8"?>
<!-- Orbital Frontier — Android manifest (scaffold).
     The launcher activity is a placeholder; once AndroidLauncher extends
     AndroidApplication, point android:name at it and confirm the namespace
     in android/build.gradle.kts. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:allowBackup="true"
        android:supportsRtl="true">

        <!-- TODO: set android:name=".AndroidLauncher" once it extends AndroidApplication. -->
        <activity
            android:name=".AndroidLauncher"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
