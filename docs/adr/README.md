# Architecture Decision Records

A dated, numbered log of significant technical decisions for Orbital Frontier and
the reasoning behind them. ADRs are append-only history: when a decision changes,
add a **new** ADR that supersedes the old one rather than rewriting it.

An `Accepted` ADR is binding unless a later ADR supersedes it. `PROJECT_BRIEF.md`
still wins on any conflict â keep them in sync.

## Index

| # | Title | Status |
|---|---|---|
| [0001](0001-engine-choice.md) | Game engine / framework choice (libGDX + Kotlin) | Accepted |
| [0002](0002-persistence-sqlite-migrations.md) | Persistence: SQLite from the start with versioned migrations | Accepted |
| [0003](0003-persistence-access-layer-sqldelight.md) | Persistence access layer: SQLDelight | Accepted |
| [0004](0004-inter-sector-travel-fixed-gates.md) | Inter-sector travel: fixed jump gates | Accepted |
| [0005](0005-movement-integration.md) | Movement integration: pure velocity model + Box2D as integrator | Accepted |
| [0006](0006-determinism-and-playthrough-harness.md) | Determinism and the playthrough record/replay harness | Accepted |
| [0007](0007-trading-prices.md) | Trading model & fixed authored prices (reconstructed, not row-persisted) | Accepted |
| [0008](0008-fleet-and-outfitting-persistence.md) | Fleet & outfitting: derived stats, junkyard-as-kind, additive v7 persistence | Accepted |
| [0009](0009-scanning-and-hidden-contacts.md) | Active scanning & hidden contacts: shared Contact capability + additive v8 persistence | Accepted |
| [0010](0010-crew-and-turret-operability.md) | Crew: persisted per-ship count, derived capacity, one-time hire, turret operability as a pure flag | Accepted |
| [0011](0011-missions.md) | Missions: deterministic static-state instancing, regenerate-and-filter persistence, virtual courier parcel, tick-authoritative timer | Accepted |
| [0012](0012-real-time-combat.md) | Real-time combat: transient encounter state, durable per-ship damage, additive v11 persistence | Accepted |
| [0013](0013-factions-and-reputation.md) | Factions & reputation: action-driven standing, separate gate filter, additive v12 persistence | Accepted |
| [0014](0014-owned-stations.md) | Player-owned stations: player state in WorldState, station-build capability flag, additive v13 persistence | Accepted |
| [0015](0015-ui-scale-and-universal-object-glyphs.md) | UI Ã2 scale knob (UI/HUD only) + universal in-world object glyphs (exhaustive POI glyph resolver) | Accepted |
| [0016](0016-on-foot-station-mode.md) | On-foot station mode: transient non-persisted prototype, shop via existing TradeScreen | Accepted |
| [0017](0017-typography-and-scalable-font.md) | Typography: pre-baked bitmap game font (JetBrains Mono OFL), bake-time-only freetype, downscaled at runtime | Accepted |
| [0018](0018-ui-skin.md) | Finished UI skin: runtime palette-driven nine-patch chrome (rename PlaceholderControlsSkin → OrbitalUiSkin) | Accepted |
| [0019](0019-world-sprite-sizing.md) | Per-type world sprite sizing (WorldSpriteSizes single source of truth); no new art produced | Accepted |
| [0020](0020-audio-system.md) | Audio: event-driven AudioService port (pure core stays audio-free), libGDX impl in render/, looping thrust + added stopSfx, idempotent music, additive v14 settings migration, placeholder synthesised clips | Accepted |
| [0021](0021-pause-overlay.md) | In-flight pause overlay: pure PauseState gate freezes the per-frame advance (inverse of the LIVE map overlay), Android back → pause, held-input + THRUST neutralization, durable quit-to-main-menu | Accepted |
| [0022](0022-ship-destruction-screen.md) | Ship-destruction screen: respawn-with-penalty (no permadeath), pure DestructionSummary + DestructionState gate nested under pause, durable onCriticalEvent flush (no schema bump), game-start fallback respawn | Accepted |
| [0023](0023-notification-event-feed.md) | In-game notification feed: pure libGDX-free notify model (purity-guarded), two-level flood defense (per-tick combat→null + drop/refresh coalescing), event-driven enqueue from existing seams, top-centre toast band, transient-MVP / persistent-feed deferred | Accepted |
| [0024](0024-first-run-tutorial-onboarding.md) | First-run tutorial & onboarding: pure libGDX-free step machine observing existing event seams (determinism-safe), draw-only hint overlay with visual-only control highlight, cross-screen two-part copy, additive v15 settings flag, replayable from settings | Accepted |
| [0025](0025-settings-screen.md) | Full settings screen: one shared grouped SettingsPanel hosted by the in-flight overlay + a new main-menu screen; joystick sensitivity/deadzone tuning applied only at the joystick boundary (determinism-safe); UiScale promoted to a clamped mutable persisted control (rendering-only, on-rebuild HUD boundary); additive v16 settings migration; UC38/UC39 groups documented-omitted not faked | Accepted |
| [0026](0026-save-slots.md) | Save slots: `slot_id` partition column in the single DB (over per-slot files/blobs); first table-rebuild migration (v16→v17) backfilling the legacy save into slot 0; `meta.active_slot_id` pointer; minSdk-24-safe seed-then-targeted-UPDATE name-clobber guard (no UPSERT); injected wall-clock for last-saved; play-time on WorldState outside replay equality | Accepted (supersedes ADR 0002 in part) |
| [0027](0027-accessibility-options.md) | Accessibility options: mode-aware `Palette` (Okabe-Ito colourblind-safe state tokens + `CONTACT_HOSTILE`/`STATION_FRIENDLY` markers, cached-per-mode immutable Colors, neutrals/accents untouched); `TextScale` UI-text knob on top of `UiScale` (HUD excluded, ≤1.31× blur ceiling); `MotionPreference` full parallax stop; pure unit-tested `FactionColors` resolver (not-yet-rendered); additive minSdk-24-safe v17→v18 settings migration; all rendering-only (determinism untouched) | Accepted |
| [0028](0028-purchase-confirmation-and-economy-feedback.md) | Purchase confirmation + economy feedback (UC40): pure `PurchaseGate` with a single `[TUNE]` 1000-credit confirm threshold (proceed/confirm/insufficient; resolvers keep authority over whether an action happens); new `ERROR` severity tier (`Palette.DANGER` red, distinct from amber `WARNING` spends) + `INSUFFICIENT_CREDITS`/`ACTION_REJECTED` kinds; sells/refuel bypass the gate (keyed on spend cost); `NotificationQueue` hoisted to one shared instance injected into PlayScreen + four desks + hub; pure `visibleWithProgress()` life-fraction snapshot drives renderer fade/drift (`visible()` kept; notify package stays engine-free) | Accepted |
| [0029](0029-combat-bounty-missions.md) | Combat / bounty missions (UC41): `MissionType.BOUNTY` with one canonical `targetZoneId` as offer-id / spawn-zone / kill-attribution key; offered identically on board + radio; edge-spawn via the dormant `EncounterSpawner.missionSpawn` hook; auto-complete-and-pay on the final kill (no station turn-in, `tryComplete` → null) and no failure timer (mining-style); pure `BountyTracking.applyKills` resolver (reputation threaded unchanged — UC43 seam); lockstep PlayScreen + test-set Simulation wiring (per-orchestrator spawn seed, no new RNG); additive minSdk-24-safe v18→v19 mission-table migration; bounty zones authored disjoint from natural encounter zones | Accepted |
| [0030](0030-loot-and-salvage-economy.md) | Loot & salvage economy (UC42): destroyed hostiles drop **transient** `SalvageDrop`s (excluded from the save, reconstruct empty on reload like `CombatState` — no schema bump, v19 unchanged); caller-side seed-deterministic loot via `LootTable.roll` keyed `salvage:$zoneId:$hostileId` on the shared `DeterministicRng` (no combat-RNG draws → 14 combat fixtures byte-identical); proximity pickup (`Salvage.collect`, `CombatParams.salvagePickupRadius`) — credits via the `applyCreditChange` chokepoint, resources via `Cargo.add` partial fill, overflow left-behind + reused CARGO-FULL toast; bounty + salvage **stack** as distinct credit sources (no double-count); `CombatParamsDto.salvagePickupRadius` is `@EncodeDefault(NEVER)`; lockstep PlayScreen + test-set Simulation wiring (collect before the combat branch, spawn threaded into both return paths) | Accepted |
| [0031](0031-combat-driven-reputation.md) | Combat-driven reputation (UC43): archetype→faction attribution (`HostileArchetype.factionId`, RAIDER/SCAVENGER stay unaligned) + negative-on-kill `ReputationParams.combatKillDelta` applied through the existing `Reputation.with` seam; pure lockstep `CombatReputation.applyKills` resolver (same-instance no-op when no faction kill); one new Independents archetype (`INDEPENDENT_MARAUDER`) + one new natural encounter zone in Gamma (no committed fixture roams Gamma, so per-sector spawn check leaves all fixtures byte-identical; Alpha keeps its single zone); single-faction MVP (no relationship graph, allied/rival propagation deferred); new `REPUTATION_CHANGED` WARNING toast (severity-keyed → no renderer change, `notify` stays faction-decoupled); `ReputationParamsDto.combatKillDelta` is `@EncodeDefault(NEVER)` so no schema bump (v19 unchanged); lockstep PlayScreen + test-set Simulation wiring | Accepted |
| [0032](0032-combat-hud-feedback.md) | Combat HUD feedback (UC44): pure derived `CombatHudState` (lock reuses `TargetingPriority.selectTarget` at the player's kinematic firing point, suppressed when no operable turret / TURRET disabled) + nearest-`MAX_BARS` enemy hull bars with distance fade; pure deterministic `CombatFeedback` (dealt/taken/shake intensities decaying to 0, no RNG/wall-clock) with reduced-motion (UC39) injected as a **parameter** so pure tests need no global `reset()`; world-space `ShapeRenderer` bars/reticle + full-screen damage flash (NOT scene2d actors → no FIRE-arc/HUD overlap, AC#4) + non-accumulating camera shake rebuilt from the ship each frame; particles omitted (flash+shake satisfy AC#3); HUD only **reads** the model → no schema bump (v19 unchanged), replay byte-identical; unit-tested derivation + source-anchored PlayScreen wiring guard | Accepted |
| [0034](0034-dynamic-station-pricing.md) | Dynamic station pricing (UC46): bounded blend — player-driven supply/demand (`StationMarketState` net signed pressure, the always-on primary mechanism) × small seeded drift (`DeterministicRng` keyed `price:<station>:<resource>:<epoch>`, **exactly 1.0 at epoch 0**, `driftAmplitude=0` kill-switch, **no seed mix**) × faction/reputation (`FactionPricing.adjust`, **exactly 1.0 at neutral**) — combined multiplier clamped to `[minMul,maxMul]` + `decayed` mean-reversion recovery (anti-degenerate-loop); **persist pressure only** (drift/decay recomputed from tick, ADR-0007 philosophy) in a new per-slot `station_market` table (additive minSdk-24-safe v19→v20 migration, regenerated `20.db`, no UPSERT, full delete-then-insert like `reputation`); effective market computed ONCE in `trade()` for both display + charge; all new DTO/state fields `@EncodeDefault(NEVER)`+EMPTY ⇒ identity at pressure-0/tick-0/neutral ⇒ uc08 still 800, **zero fixture regen**; supersedes the dynamic-pricing deferral in ADR 0007 + the FactionPricing-deferral in ADR 0013; per-seed variance deferred (needs a persisted world seed) | Accepted (supersedes ADR 0007 / ADR 0013 in part) |
| [0035](0035-junkyard-buy-used-parts.md) | Junkyard buy-used parts (UC47): data-driven flat used-part discount (`UsedPartParams.discountFraction` default 0.6, deliberately above the 0.5 sell-refund so buy-used→sell always loses money — no arbitrage) installing through the SAME `Loadout.install` path as a new part; **deterministic baseline + persisted depletion** stock model (baseline a pure `DeterministicRng` draw keyed `usedstock:<junkyard>:<part>` in `[minStock,maxStock]`, only the purchased count persisted ⇒ `available = baseline − purchased`, killing the reload-restock exploit; **no time-based restock** — deferred); compile-enforced **anti-exploit invariant** via a non-defaulted `OutfitResult.junkyardStock` (only a successful `BuyUsed` mutates it, every other path threads the input through); per-junkyard authored `Station.usedPartMarket` (separate from the refit `outfitMarket`); additive minSdk-24-safe v20→v21 `junkyard_stock` migration + regenerated `21.db`, full delete-then-insert like `station_market`; all new state/DTO/config fields `@EncodeDefault(NEVER)`+EMPTY ⇒ **zero fixture regen**; `UsedPartParams` pinned per-playthrough; restock cadence + condition/wear + per-seed variance deferred | Accepted |
| [0036](0036-reputation-gated-acquisition.md) | Reputation-gated upgrade & ship acquisition (UC48): authored `unlockThreshold` per catalog item × the **docked station's faction** (the `FactionPricing` key) — derived **at read time** from live `reputation`, so **no schema bump / migration / fixture regen** and AC#3 (persist + update-as-standing-changes) falls out for free; new pure `StandingGate.status` (≥-threshold availability + the AC#4 "why locked" payload, mirroring `ReputationGate`); price via the UC46 seam — new `FactionPricing.adjustedPrice` (single source of truth, exactly base at neutral) charged by the resolvers AND shown by the screens (**display == charge**); gating enforced in the **pure resolvers** (`Outfitting`/`FleetResolver`, neutral-default params) not just UI, for **live == replay** parity (BuyUsed composes faction-adjust → used discount); locked items render as **visible disabled rows** (not vanishing, AC#4); **no confiscation** — gate consulted only on Buy availability, never on RemoveSell/Switch/load/stat-derivation; authored gate at standing 10 on Beta tier-II parts `ENGINE_TUNE_II`/`CARGO_POD_II` + league hull `PROSPECTOR` (10 ≤ `missionCompleteDelta`, one league mission unlocks); all new logic purely derived, **no new RNG draws** ⇒ byte-identical at neutral; lockstep PlayScreen + test-set Simulation wiring; per-station thresholds + allied/rival propagation deferred | Accepted (supersedes ADR 0013 in part) |
| [0033](0033-richer-enemy-ai-and-spawn-director.md) | Richer enemy AI & spawn director (UC45): five additive, opt-in, seed-deterministic decisions — (1) `AiBehavior.RETREAT_AND_REGROUP` as an *additive* `EnemyAi.decide` branch (AGGRESSIVE/FLEE left byte-identical) + opt-in archetype flags `regroupRange`/`targetsWeakestSection`; (2) deterministic **"weakest-at-fire-time"** section targeting (`WeakestSection`: lowest current-HP fraction, `ShipSection.name` tie-break) stamped on the projectile and applied **directly with no RNG draw** — non-targeting (all existing) hostile fire keeps the RNG-weighted `DamageModel` path so the stream stays byte-identical; (3) progression-driven opt-in `SpawnDirector`/`SpawnScaling` (`None` default = base unchanged for any input; `ByProgression` appends bounded reinforcements; input = `ProgressionLevel.of(installedUpgradeCount)` over reputation to keep archetypes unaligned + seed difficulty from loadout; lockstep PlayScreen + test-set Simulation); (4) `EncounterZone.composition: List<HostileSpawn>` with a backward-compatible secondary ctor + computed `archetypeId`/`hostileCount` (single-entry composition draws RNG byte-identically; `missionSpawn` stays single-archetype); (5) transient state, no schema bump (v19). New `REGROUP_MARAUDER`/`PRECISION_RAIDER` archetypes unaligned (`factionId = null`, CONDITION #2); new Beta + Gamma zones authored disjoint from every existing fixture path (CONDITION #1); formations deferred (no AC) | Accepted |
| [0037](0037-power-brownout-budget.md) | Power brownout (UC49): pure rate-based **power budget** with automatic priority shedding (`power/Brownout.resolve` → transient `BrownoutResult`) and a **protected HELM floor never shed** (no-deadlock by construction — thrust/helm always powered; degenerate protected-over-output case sheds all sheddable + `isBrownout=true`); shed order SCANNER → WEAPONS; new budget-only `PowerParams.weaponsDraw`/`scannerDraw` (default 0) feed the **budget ONLY, NOT `PowerModel.drawAt`/fuel burn** ⇒ budget-demand (base+thrust+weapons+scanner) diverges from fuel-burn-demand (base+thrust), preserving UC16 25/75 tuning; suppression threaded through the shared `Combat.step(…, weaponsPowered)` (shed WEAPONS → no player fire) + `Scanning.resolve(…, scannerPowered)` (shed SCANNER → no scan), both defaulting true; lockstep PlayScreen + test-set Simulation compute brownout from the SAME `thrusting` bool fed to fuel burn; HUD `PWR <pct>%`/brownout cue is render-only (pure `HudViewModel` power fields); **transient — no schema bump (v21)**, `PowerParamsDto.weaponsDraw`/`scannerDraw` are `@EncodeDefault(NEVER)` ⇒ zero fixture regen (UC07_THIRSTY_POWER now `isBrownout` render-only but byte-identical); capacitor (AC#2) deferred | Accepted |
| [0038](0038-crew-depth-and-wages.md) | Crew depth, wages & fleet/crew management (UC50): crew **identities** (`CrewMember`/`CrewId`/`CrewRole` {PILOT,GUNNER,ENGINEER,DECKHAND, inert metadata in MVP} + `CrewRoster`/`CrewAssignment`) as a **production-only overlay** on `WorldState.crewRoster` — the per-ship **count** (`OwnedShip.crew`) stays authoritative + deterministic, identities never touch `SimulationState`/`OwnedShipDto` ⇒ structural byte-identity, invariant `roster.forShip(s).size == s.crew` reconciled-from-counts on load (synthesize generic members, no migration backfill); **default-OFF wages** (`WageParams` rate 0 ⇒ same-value no-op ⇒ zero fixture regen) keyed on the integer tick (`isWageTick`) in lockstep PlayScreen (`wageTickAccumulator`) + test-set Simulation (`creditsAfterWages` threaded into both branches), clamp-at-0 **unpaid rule** (no debt / no desertion, `UNPAID_WAGES` WARNING toast) routed through `applyCreditChange`; thin `FleetCrewScreen` (primary switch surface, reuses `FleetResolver.switchActive` — the bare `ShipyardScreen` switch is **kept**, a recorded divergence from the literal "replace"); additive minSdk-24-safe v21→v22 `crew_member` table + regenerated `22.db` (no UPSERT, full delete-then-insert like `reputation`); `Playthrough.wageConfig` `@EncodeDefault(NEVER)`; wage-accumulator phase not persisted (only drained credits persist — AC#2 met); refit crew re-clamp a dead path (no crew-quarters upgrade in `UpgradeCatalog.MVP`, pinned by a guard); crew skills/upgrades + desertion/debt deferred | Accepted (supersedes ADR 0010 in part) |
| [0039](0039-station-build-ui-surfacing-and-dock-to-use.md) | Station build UI, world-surfacing & dock-to-use (UC51): **project, don't author; compose, don't fork; no schema bump (v22 stays)** — pure `OwnedStationProjection` turns each `OwnedStation` into a synthetic `Station` POI (`owned-station-<id>`, derived position, "Outpost N", commerce desk iff COMMERCE / retrofit desk iff RETROFIT, all other caps off) consumed as `effectivePois = sector.pois + projection` + a single `resolveDocked(...)` ⇒ **zero renderer/`MapLabels`/`Docking`/desk changes**, honors ADR 0014 (never authored into the fixed graph); **placement decided** (ADR 0014 open question): fixed per-sector lattice fanned by id near `(0,-600)`, injective ⇒ pairwise-distinct for arbitrary N + clear of Alpha `(0,600)` / `alpha-raider-picket` `(900,0,r260)` (challenger #1); placement a pure fn of id + reconstructed markets ⇒ re-derived byte-identically on reload, **no placement column / migration / `.db` regen** (save→reload placement-equality, challenger #2); new `StationBuildScreen` over pure `StationBuildMenu` (found + module×owned expansion options, cost preview, shared `StationBuildCost.canAfford` single source of truth) replacing UC15's direct default build; **compose** — owned stations reuse `StationHubScreen` gated by a new `HubService` enum + `enabledServices` (default = full set ⇒ authored button set byte-identical/guard-pinned; owned = {TRADE/OUTFIT}+UNDOCK, no duplicated refuel/missions/crew, pitfall #4); owned station transponder ⇒ unconditional on minimap + overlay (challenger #4); strictly additive/default-off ⇒ zero owned ⇒ `effectivePois==sector.pois` + docked lookup unchanged ⇒ **every existing fixture byte-identical** (only new `uc51-owned-station` fixture exercises the new path); lockstep `PlayScreen` + test-set `Simulation` via the same pure projection; respawn-at-owned-station / defense / passive income / crew-staffing / teardown deferred | Accepted |
| [0040](0040-autosave-cadence-and-save-robustness.md) | Autosave cadence, indicator & save robustness (UC52): **save mechanics, not shape — no schema bump (v22 stays)** — robust DB open via pure, JVM-testable `java.io` core seams: `SaveSchemaProbe` reads the SQLite header directly (16-byte magic + 4-byte big-endian `user_version` at offset 60, no DB open) so a NEWER save is recognised without a crashing downgrade-open; `SaveBackupStore` makes a rolling last-good `.bak` via atomic temp-then-rename; `SaveDatabaseOpener` orchestrates backup-before-migrate + rollback with a **distinct injectable `forceOpen`** (default `PRAGMA user_version`) called *inside* the try/catch so Android's **lazy** migration throws where rollback catches it; `v > current` ⇒ **UnsupportedNewer** (never opened/downgraded), unreadable/corrupt ⇒ restore-from-`.bak` else a non-crashing **main menu with Continue disabled + explanatory notice** (New Game keeps its double-confirm ⇒ a newer save is never silently clobbered, discarded only via a fresh-DB recovery path); **`journal_mode=DELETE`** set via `setWriteAheadLoggingEnabled(false)` on the Android `SupportSQLiteOpenHelper` (not a callback PRAGMA) so the single `.db` is authoritative (probe + single-file backup correct by construction); **data-driven cadence** (`AutosaveParams.periodicIntervalSeconds`, 20s); **render-only autosave indicator** (`AutosaveIndicatorState` IDLE/SAVING/SAVED + fade, ASCII label) bridged from the off-thread writer by `AutosaveActivitySignal` (two `AtomicLong` counters, markSaving render-thread / markSaved executor-thread / poll per frame), drawn bottom-right by `HudRenderer` — lives in the frame loop **off `SimulationState`** ⇒ replay byte-identical; new `androidx.sqlite:sqlite-framework` compile dep (already transitive at runtime); WAL-off + lazy-migration rollback only fully verifiable on a real Android open (QA asserts on a real DELETE-mode file); generational backup history deferred | Accepted |
| [0041](0041-procedural-sector-generation.md) | Procedural sector generation (UC53): **additive generator, authored map stays the default — zero fixture regen by construction** — `SectorGenerator.generate(seed)` returns `MvpSectorMap.build()` **verbatim** for the reserved `WorldSeed.MVP = 0` and procedurally generates only for a non-zero seed; `WorldState.worldSeed` defaults to MVP + the new `game_state.world_seed INTEGER NOT NULL DEFAULT 0` column ⇒ every existing save / fixture / new game resolves to the authored map (fixtures carry no world graph, the test `Simulation` default + the `MvpSectorMap` call site are untouched); generation is pure (`DeterministicRng` fnv1a→LCG only — no wall-clock / `Math.random` / identity hashCode) so a replay can be re-pinned to a fixed seed (AC#4); **connectivity is a generator invariant** — sectors wired into a **ring** of reciprocal gates (N=3 ⇒ the authored triangle) + optional seed-decided chord for N≥4, so every sector is reachable (a mandatory BFS reachability test pins it; `SectorWorld` re-validates reciprocity + global id uniqueness but NOT reachability); **templates + jitter** (the seeded-but-curated decision) — per sector 1 station + 1 belt + 0–1 hidden contacts, seeded angle/radius placement, globally-unique ids; `worldSeed` is **NOT** in `SimulationState` (replay equality untouched) and `PlayScreen.currentWorldState()` re-emits it so autosave never reverts the seed to MVP (AC#3); additive minSdk-24-safe v22→v23 `world_seed` migration (`22.sqm`) + regenerated `23.db`; **scope boundary** — a non-MVP world gets templated markets only, NO encounter/bounty content (those tables key on literal `"alpha"`/`"beta"`/`"gamma"` ids; deferred follow-up), as are density/parameter tuning + any new-random-world UI | Accepted |

## Adding an ADR

1. Copy [`_TEMPLATE.md`](_TEMPLATE.md) to `NNNN-<slug>.md` using the next number.
2. Fill in Context / Options / Decision / Consequences; set `Status` and `Date`.
3. Add a row to the table above.
4. To change a past decision, create a new ADR and set the old one's status to
   `Superseded by ADR-NNNN`.
