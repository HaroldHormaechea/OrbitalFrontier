# Gradle
.gradle/
build/
**/build/
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties

# Android
*.apk
*.aab
*.ap_
*.dex
local.properties
.cxx/

# Signing / secrets (never commit)
*.jks
*.keystore
keystore.properties
*.p12
play-credentials*.json
*.env
.env*

# IDE
.idea/
*.iml
.vscode/
*.hprof

# OS
.DS_Store
Thumbs.db

# libGDX packed assets (regenerated)
assets/**/*.atlas
assets/**/packed/

# UC27: the delivered design-system atlas is a committed source asset, NOT a regenerated pack —
# un-ignore it (and its page PNG) so it ships in the repo and the APK. Keep the generic rule above
# so any future *packed* atlas under assets/ stays ignored.
!assets/orbital.atlas
!assets/orbital.png

# Dev-team QA visual-verification scratch (screenshots; not source)
qa-artifacts/
