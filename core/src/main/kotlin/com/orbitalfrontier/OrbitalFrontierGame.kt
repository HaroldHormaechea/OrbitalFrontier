package com.orbitalfrontier

/**
 * Game entry point for Orbital Frontier.
 *
 * In the libGDX layout this will implement `com.badlogic.gdx.ApplicationListener`
 * (or extend `com.badlogic.gdx.Game`) and own the screen lifecycle:
 *   - create():   load assets, restore save (or start a new game), set initial screen
 *   - render():   advance the active screen / game loop each frame
 *   - dispose():  release GPU and asset resources
 *
 * Scaffold stub only — no engine dependency wired up yet. Replace the body once
 * libGDX is on the classpath (see core/build.gradle.kts).
 */
class OrbitalFrontierGame {
    fun create() {
        // TODO: initialize subsystems (rendering, input, mission system,
        //       progression, save/load) and switch to the menu/play screen.
    }
}
